import { TalkToExpertDetails } from "@/components/dashboard/talk-to-expert/talk-to-expert-details";

export default function TalkToExpertDetailPage({ params }) {
  return <TalkToExpertDetails params={params} />;
}
